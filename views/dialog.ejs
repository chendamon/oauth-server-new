<!DOCTYPE html>
<html>
  <head>
    <title>用户授权</title>
    <link rel="icon" type="image/x-icon" href="/images/infinity.ico">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body class="white">
    <div class="col-sm-offset-4 col-md-offset-4 col-sm-4 col-md-4 auth-block">
      <h4 class="posi">&nbsp; &nbsp;<b><%= user.username %></b>您好，应用<b><%= client.clientId %></b>正在申请访问您的账户信息，是否同意？</h4>
        <form class = "col-sm-offset-3 col-md-offset-3 col-sm-6 col-md-6" style="height:36px;margin-top:40px;" action="/oauth/authorize" method="post">
          <input type="hidden" name="client_id" value=<%= client.clientId %> />
          <input type="hidden" name="redirect_uri" value=<%= redirect_uri %> />
          <input type="hidden" name="response_type" value=<%= response_type %> />
          <input type="hidden" name="state" value=<%= state %> />
          <input class="btn btn-danger" style="height:36px;width:auto;float:left;" type="submit" name="allowed" value="不同意" id="deny"/>
          <input class="btn btn-success" style="height:36px;width:auto;float:right;" name="allowed" type="submit" value="同意" id="allow" />
        </form>
    </div>
  </body>
</html>
